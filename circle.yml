machine:
  node:
    version: 5.7.1

test:
  pre:
    - echo ${FOO}

deployment:
  beta-dev-heroku:
    branch: master
    heroku:
      appname: citizenship-appointment-beta
  post:
    - BROWSER=chrome SAUCE_ON_DEMAND=true npm run cross-browser-test
